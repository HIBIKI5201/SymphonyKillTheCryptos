### **計画書**
**タイトル:** これまでのデバッグログの削除

**目的:**
これまでに原因究明のために追加したデバッグログを削除し、コードをクリーンな状態に戻す。

**作業内容:**
1.  `Assets/Scripts/Runtime/UseCase/Ingame/System/InGameLoopUseCase.cs` から追加したデバッグログを削除する。
2.  `Assets/Scripts/Runtime/UI/Ingame/HUD/IngameUIManager.cs` から追加したデバッグログを削除する。

**変更ファイル:**
*   `Assets/Scripts/Runtime/UseCase/Ingame/System/InGameLoopUseCase.cs`
*   `Assets/Scripts/Runtime/UI/Ingame/HUD/IngameUIManager.cs`

**変更内容:**

`Assets/Scripts/Runtime/UseCase/Ingame/System/InGameLoopUseCase.cs`:

```csharp
// 変更前
                if (_levelUseCase.LevelUpQueue.Any())
                {
                    Debug.Log("InGameLoopUseCase: レベルアップフェーズ開始"); // 追加
                    _levelUpPhaseHandler.OnLevelUpPhaseStarted();
                    while (_levelUseCase.LevelUpQueue.TryDequeue(out var newLevel))
                    {
                        Debug.Log($"InGameLoopUseCase: レベルアップ処理中: Level {newLevel}"); // 変更
                        await _levelUseCase.HandleLevelUpAsync(_onLevelUpSelectNodeCallback);
                    }
                    _levelUpPhaseHandler.OnLevelUpPhaseEnded();
                    Debug.Log("InGameLoopUseCase: レベルアップフェーズ終了"); // 追加
                }

// 変更後
                if (_levelUseCase.LevelUpQueue.Any())
                {
                    _levelUpPhaseHandler.OnLevelUpPhaseStarted();
                    while (_levelUseCase.LevelUpQueue.TryDequeue(out var newLevel))
                    {
                        Debug.Log($"InGameLoopUseCase: レベルアップ！ 新しいレベル: {newLevel}");
                        await _levelUseCase.HandleLevelUpAsync(_onLevelUpSelectNodeCallback);
                    }
                    _levelUpPhaseHandler.OnLevelUpPhaseEnded();
                }
```

`Assets/Scripts/Runtime/UI/Ingame/HUD/IngameUIManager.cs`:

```csharp
// 変更前
        public async Task<LevelUpgradeNodeViewModel> LevelUpSelectAsync(Memory<LevelUpgradeNodeViewModel> nodes)
        {
            Debug.Log($"IngameUIManager: LevelUpSelectAsync が呼び出されました。選択肢の数: {nodes.Length}"); // 追加
            Debug.Log($"候補カード {string.Join(", ", nodes.ToArray().Select(n => n.NodeName))}");

            // ウィンドウを出現させて待機。
            OpenLevelUpgradeWindow(nodes.Span);
            Debug.Log("IngameUIManager: OpenLevelUpgradeWindow を呼び出しました。"); // 追加

            LevelUpgradeNodeViewModel selectedNodeVM = default;
            await SymphonyTask.WaitUntil(
                () => TryGetSelectedLevelUpgradeNode(out selectedNodeVM));

            Debug.Log($"レベルアップカードを選択しました。{selectedNodeVM.NodeName}");

            CloseLevelUpgradeWindow();
            Debug.Log("IngameUIManager: CloseLevelUpgradeWindow を呼び出しました。"); // 追加

            return selectedNodeVM;
        }

// 変更後
        public async Task<LevelUpgradeNodeViewModel> LevelUpSelectAsync(Memory<LevelUpgradeNodeViewModel> nodes)
        {
            Debug.Log($"候補カード {string.Join(", ", nodes.ToArray().Select(n => n.NodeName))}");

            // ウィンドウを出現させて待機。
            OpenLevelUpgradeWindow(nodes.Span);

            LevelUpgradeNodeViewModel selectedNodeVM = default;
            await SymphonyTask.WaitUntil(
                () => TryGetSelectedLevelUpgradeNode(out selectedNodeVM));

            Debug.Log($"レベルアップカードを選択しました。{selectedNodeVM.NodeName}");

            CloseLevelUpgradeWindow();

            return selectedNodeVM;
        }
```

---
**完了レポート:**
`Assets/Scripts/Runtime/UseCase/Ingame/System/InGameLoopUseCase.cs` および `Assets/Scripts/Runtime/UI/Ingame/HUD/IngameUIManager.cs` に追加されていたデバッグログを削除し、コードをクリーンな状態に戻しました。